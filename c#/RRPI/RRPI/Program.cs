namespace RRPI
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            /*
            //입력
            Console.Write("반지름은:");
            int radius = int.Parse(Console.ReadLine());

          
            //기능
            float answer = (float)(Math.PI * Math.Pow(radius, 2));//r * r
            //출력
            Console.Write("넓이는:");
            Console.WriteLine(answer); */
            byte a = -254;
            Console.WriteLine(a);
        }
    }
}